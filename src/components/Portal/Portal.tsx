import { Alert } from "@mui/material";

export default function Portal(): JSX.Element {
  return (
    <Alert severity="success">
      Welcome to the uOttawa portal, you are logged in!
    </Alert>
  );
}
